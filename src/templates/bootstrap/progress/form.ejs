<div style="width:{{ctx.component.width}};height:{{ctx.component.height}};display: flex;flex-direction: column;gap: 10px">
    <div ref="titleContainer" style="display: flex;flex-direction: column;gap: 10px">
    </div>
    <div ref="progressContainer" class="progress" style="background-color: {{ ctx.component['progress-color'] }}">
    </div>
</div>
